services:
  - type: web
    name: mountain-goats
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.20.4
      # Add in the Render dashboard (Neon, Supabase, or Render Postgres):
      # - key: DATABASE_URL
      #   sync: false

